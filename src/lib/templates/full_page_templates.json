[
  {
    "id": "full_editorial_corners",
    "description": "Editorial hero with content anchored to the corners of the viewport (headline bottom-left, description right, CTA bottom-right), tall scroll spacers between sections, a 3-column offset feature grid, and a glassmorphism footer. Cinematic, premium, sporty/luxury feel.",
    "prompt_template": "LAYOUT SKELETON — structure and motion only. All names, copy, fonts, and colors come from the Build Brief above; this skeleton contains none.\n\nPAGE STRUCTURE (App.tsx):\n<ScrollFrames /> first, then a fixed header, then <main className=\"relative z-10\"> containing: Hero (100dvh) → Feature section → Details section → Footer. Sections sit DIRECTLY adjacent — no spacer divs anywhere.\n\nFIXED HEADER: logo/wordmark left; 4-5 small uppercase tracking-widest nav links right; one small pill CTA. Transparent background, no border. Nav links smooth-scroll to section ids.\n\nHERO (h-[100dvh], w-[90%] mx-auto, flex column): a 12-column grid on desktop. Oversized display headline (clamp, 2 lines, tight leading) anchored bottom-left spanning ~7 columns; a short description paragraph mid-right (~4 columns, small text, 60% opacity); a two-part pill CTA bottom-right (label segment + arrow segment with a 1px gap). Elements fade-up in with a small stagger on load. The hero content (headline, description, CTA) is visible in the FIRST viewport, layered over the video — the page never opens on an empty viewport of bare video, and no spacer comes before the hero.\n\nFEATURE SECTION (min-h-[100dvh], centered content, max-w-6xl): a small uppercase eyebrow naming the topic, one large statement heading that reveals word-by-word on scroll (Framer Motion whileInView with staggered spans), then a 3-column grid where the middle column is offset vertically (translate-y) for asymmetry. Each cell: thin 1px top border, small numbered label, short title, 2-line description. No card backgrounds — transparent with borders only.\n\nDETAILS SECTION (min-h-[100dvh], w-[90%] mx-auto, flex items-center): a 2-column split — left: a large statement heading (sticky top-24 on desktop); right: 3-4 numbered rows, each a 1px-top-border block (two-digit number, short title, one-line description) fading in sequentially whileInView.\n\nFOOTER (final section, sits at the absolute bottom of the scroll): a large glassmorphism panel (backdrop-blur, translucent dark fill, rounded top corners) containing a big CTA headline + pill button, a 4-column link grid, and a thin copyright bar with a 1px top border.\n\nMOTION: Framer Motion only. Fade/slide whileInView reveals, hover states on nav (label slides up, duplicate slides in from below). Nothing bouncy."
  },
  {
    "id": "full_sticky_statement",
    "description": "A sticky centered hero with a huge split headline (top-left + bottom-right), character-reveal text animation, a floating pill header, and a bottom progressive fade. Feels techy, cinematic, high-contrast. Good for AI/tech/science brands.",
    "prompt_template": "LAYOUT SKELETON — structure and motion only. All names, copy, fonts, and colors come from the Build Brief above; this skeleton contains none.\n\nPAGE STRUCTURE (App.tsx):\n<ScrollFrames /> first, then a fixed header (z-50), then <main className=\"relative z-10\"> containing: Sticky hero (200vh) → Statement section → Detail section → Footer. After the hero, sections sit DIRECTLY adjacent — no empty spacer divs.\n\nFIXED HEADER (h-20, transparent, fades in on load): left — a small translucent pill (bg-white/15 backdrop-blur-md rounded-2xl px-5 h-12) holding a simple inline-SVG glyph logo + wordmark; right — one solid pill CTA button (h-12 rounded-full) whose label plays a hover text-decode effect. Subtle whileHover scale 1.02 / whileTap 0.98.\n\nSTICKY HERO: outer section h-[200vh]; inner wrapper sticky top-0 min-h-[80vh] max-w-7xl mx-auto flex flex-col justify-between. Top-left: display headline line 1 + line 2. Bottom-right: a second, shorter display headline. Bottom-left: a small description paragraph (max-w-sm, 60% opacity). Headlines animate in with a character-by-character reveal (spans + staggered Framer Motion variants); as the user scrolls, the whole hero content fades and scales down slightly (useScroll + useTransform: opacity 1→0, scale 1→0.96 over the first quarter of scroll). Add a faint dot-grid overlay (radial-gradient background-image via Tailwind arbitrary value, ~5% opacity) inside the hero only. The hero headlines and description are visible in the FIRST viewport, layered over the video — the page never opens on an empty viewport of bare video.\n\nPROGRESSIVE BOTTOM FADE: one fixed bottom strip (h-36, z-30, pointer-events-none) with a transparent-to-black gradient plus a light backdrop-blur — the ONLY darkening element allowed, and it never covers the full viewport.\n\nSTATEMENT SECTION (min-h-[100dvh], centered): a single max-w-4xl statement heading, word-by-word whileInView reveal, with 2-3 key words in italic or the accent color. Below it a row of 3 small stat blocks (big number, thin 1px left border, tiny uppercase label).\n\nDETAIL SECTION (min-h-[100dvh]): two-column split — sticky small heading on the left, a vertical list of 4 numbered rows on the right, each row a 1px-top-border block (number, title, one-line description) that fades in sequentially.\n\nFOOTER: slim — large wordmark, one pill CTA, single row of small links, thin top border. Transparent background.\n\nMOTION: Framer Motion only (useScroll, useTransform, whileInView, variants). No manual scroll listeners."
  },
  {
    "id": "full_floating_overlay",
    "description": "A fixed oversized typographic overlay that collapses character-by-character as you scroll, a centered floating pill navigation, and a large glass panel that slides up from the bottom with a brand marquee. Artistic, fashion/creative-studio energy.",
    "prompt_template": "LAYOUT SKELETON — structure and motion only. All names, copy, fonts, and colors come from the Build Brief above; this skeleton contains none.\n\nPAGE STRUCTURE (App.tsx):\n<ScrollFrames /> first, then <PillNav /> (fixed), then a relative container ~250vh tall containing <FloatingHeadline /> (fixed overlay) and <GlassPanel /> (absolute bottom-0), then a slim footer section.\n\nPILL NAV (fixed, centered horizontally, top-6, z-50): a circular logo pill (48px, solid fill, simple inline-SVG glyph) + an adjacent rounded pill bar with 3-4 nav items (uppercase, 13-14px, tracked). Each item is its own small pill; on hover the label slides up and a duplicate label in inverted colors slides in from below (Framer Motion). Entry: logo scales in with a spring, items fade in with slight delay. On mobile collapse items into a hamburger with a small dropdown. Nav items smooth-scroll (element.scrollIntoView({behavior:'smooth'})) — no scroll libraries.\n\nFLOATING HEADLINE: fixed inset-0, flex column justify-end, p-4 md:p-8, pointer-events-none, z-10. Two lines of display text at clamp(4rem, 15vw, 18rem), line-height 0.85. Wrap each character in an inline-block span; drive a per-character exit animation from scroll position (useScroll + useTransform, or a scroll-linked variants stagger): characters translate down, squash slightly (scaleY up / scaleX down) and fade as the user scrolls the first ~600px. The full headline is visible immediately on load in the FIRST viewport, layered over the video — the page never opens on an empty viewport of bare video.\n\nGLASS PANEL (absolute bottom-0 of the 250vh container — this container is the scroll stage for the reveal, not empty space; the fixed headline stays on screen throughout it): a max-w-6xl h-[85vh] max-h-[900px] rounded-3xl panel — translucent dark fill, heavy backdrop-blur, 1px white/10 border. It slides up from below the viewport tied to scroll progress (y: 100% → 0% via useScroll on the container). Optional gentle mouse-parallax tilt (a few px translate + 2-3deg rotate, spring-smoothed). Inside, centered: a small italic eyebrow, one very large serif-or-display statement heading (a few words italicized or accented), and at the bottom a full-width marquee strip (uppercase names separated by generous padding, translateX loop at ~20s, 40% opacity, 1px top border).\n\nFOOTER (after the container): minimal single-row footer — wordmark left, 3-4 links right, thin top border, transparent background.\n\nMOTION: Framer Motion only (useScroll, useTransform, springs). No GSAP, no scroll libraries, no manual rAF loops."
  }
]